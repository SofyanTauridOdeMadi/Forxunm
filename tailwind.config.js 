// postcss.config.js
module.exports = {
  plugins: [
    require('tailwindcss')({
      content: [
        "./html/**/*.html",   // Memindai file HTML di dalam folder html
        "./js/**/*.js",       // Memindai file JS di dalam folder js
      ],
      theme: {
        extend: {},
      },
      plugins: [],
    }),
    require('autoprefixer'),
  ],
};